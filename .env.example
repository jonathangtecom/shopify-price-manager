# Shopify Price Manager Configuration

# Session secret - generate with: python -c "import secrets; print(secrets.token_hex(32))"
SESSION_SECRET=change-me-generate-a-random-string

# Admin password hash - generate with: python scripts/hash_password.py yourpassword
ADMIN_PASSWORD_HASH=

# Database path
DATABASE_PATH=./data/app.db

# Logging level
LOG_LEVEL=INFO

# Server (usually don't need to change)
HOST=0.0.0.0
PORT=8080
